<!-- The companion stays hidden and silent until a visitor chooses Meet B-9. -->
<b9-companion id="site-robot" size="220" hidden></b9-companion>
<b9-remote for="site-robot"></b9-remote>
<script is:inline defer src="/assets/b9/b9-companion.3120719178d6.js"></script>

<style>
  b9-companion:not(:defined),
  b9-remote:not(:defined) {
    display: none;
  }

  b9-remote {
    left: max(16px, env(safe-area-inset-left));
    bottom: max(16px, env(safe-area-inset-bottom));
  }

  :global(.site-footer) {
    padding-bottom: calc(80px + env(safe-area-inset-bottom));
  }

  :global(body > .skip-link) {
    z-index: 1300;
  }

  @media print {
    b9-companion,
    b9-remote {
      display: none !important;
    }
  }
</style>
